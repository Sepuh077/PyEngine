# Core dependencies
pygame==2.6.1
numpy>=1.20.0
pillow==11.3.0
trimesh>=4.0.0
moderngl>=5.6.0

# Build-time only (for compiling Cython extensions):
# These are pulled automatically when you do `pip install .` thanks to pyproject.toml.
# You only need them if building from source.
# setuptools>=61
# Cython>=3.0
# numpy (already required at runtime too)

# Editor UI
PySide6>=6.6.0

# Dev / test
pytest==9.0.2
pytest-cov>=5.0.0
ruff>=0.8.0
